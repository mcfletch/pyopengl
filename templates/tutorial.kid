<html xmlns="http://www.w3.org/1999/xhtml" xmlns:py="http://purl.org/kid/ns#">
<table width="100%" py:def="navtable()"><thead>
	<tr>
		<td width="8em;"/><th align="center"><a href="index.html">OpenGLContext Python tutorials</a></th><td width="8em;"/>
	</tr>
	<tr>
	<td><a py:if="prev" href="${prev.relative_link}">Previous</a><a py:if="not prev" href="index.html">Index</a></td>
	<td align="center">${tutorial.title}</td>
	<td align="right"><a py:if="next" href="${next.relative_link}">Next</a><a py:if="not next" href="index.html">Index</a></td>
	</tr>
</thead></table>

<div py:def="children(node)" py:if="node.children" py:strip=""><div py:for="child in node.children" py:strip="">${convert(child)}</div></div>
<div py:def="node_content(node)" py:strip="">${node.text}${children(node)}</div>
<div py:def="convert( node )" py:strip="">
	<h1 py:if="node.html_tag == 'h1'" class="${node.html_class}">${node_content(node)}</h1>
	<div py:if="node.html_tag == 'div'" class="${node.html_class}">${node_content(node)}</div>
	<ul py:if="node.html_tag == 'ul'" class="${node.html_class}">${node_content(node)}</ul>
	<li py:if="node.html_tag == 'li'" class="${node.html_class}">${node_content(node)}</li>
	<a py:if="node.html_tag == 'a'" class="${node.html_class}" href="${node.url}">${node_content(node)}</a>
	<img py:if="node.html_tag == 'img'" class="${node.html_class}" src="${node.url}" alt="${node.text}" />
	${node.tail}
</div>
<head>
    <title>${tutorial.title}</title>
    <link rel="stylesheet" href="./tutorial.css" type="text/css" />
</head>
<body>
${navtable()}
${children(tutorial)}
${navtable()}

<div class="metadata">This document was generated by <code>bzr branch http://bazaar.launchpad.net/%7Emcfletch/pyopengl/directdocs/</code> on ${date} from ${tutorial.filename}</div>
</body>
</html>

